
* 콜랙션 ( 표준화 )
       - 기본적인 자료구조(개념)를 만들어 줌
       - java.util 패키지
       
 1. List
         - ArrayList : Object[] 동적크기  // 크기를 알아서 늘려줌
         - 순서저장(index 보유)*****
         
 2. Set
         - 순서없음 ( index 없음 )
         - 중복값 저장 안됨    // List와 다르게 방 번호가 따로 없으므로 중복값 안됨
 3. Map      
         - 키(key)와 벨류(value)  저장 구조
         - 키는 중복 안됨